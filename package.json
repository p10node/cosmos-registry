{
  "name": "@p10node/cosmos-registry",
  "version": "0.1.0",
  "main": "src/main.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "reload": "bun upgrade && bun install",
    "direct": "bun run src/main.ts",
    "lint": "biome format --write ."
  },
  "dependencies": {
    "@chain-registry/types": "^2.0.98",
    "chain-registry": "^2.0.98"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.4",
    "@types/node": "^24.10.0"
  }
}
